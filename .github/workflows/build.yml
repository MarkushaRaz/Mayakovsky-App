name: Build App

on:
  push:

jobs:
  Build:
    runs-on: windows-latest
    steps:
      - name: Run
        uses: actions/checkout@v3

      - name: Download MinGW
        run: |
          choco install mingw --forcex86 -y
          echo "C:\ProgramData\chocolatey\bin" >> $GITHUB_PATH
      
      - name: Build C++
        run: |
          g++ build/main.cpp -o Mayakovsky.exe -static -mconsole -m32

      - name: Push
        uses: actions/upload-artifact@v4
        with:
          name: Mayakovsky
          path: Mayakovsky.exe